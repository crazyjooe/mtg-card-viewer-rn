import { Theme } from '../types';

const LightTheme: Theme = {
	backgroundColor: 'white',
	linkColor: 'blue',
	red: '#ff0000',
	green: '#00ff00',
	yellow: '#ffff00',
	black: '#000000'
};

export default LightTheme;
